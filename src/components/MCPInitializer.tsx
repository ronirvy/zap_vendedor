'use client';

// Este componente não deve inicializar servidores MCP do lado do client.
// Toda inicialização de servidores deve ser feita apenas no backend/server-side.

export default function MCPInitializer() {
  // Este componente não faz nada no client
  return null;
}